<div class="all-height flex-wrap div-search">
    <div id="div-cards">
        {{#each spots}}
        <div class="card mb-3" style="max-width: 540px;">
            <div class="row no-gutters">
                <div class="col-md-4">
                    <img src="{{this.picture}}" class="card-img" alt="...">
                </div>
                <div class="col-md-8">
                    <div class="card-body">
                        <h5 class="card-title"> <a href="/spots/{{this._id}}">{{this.title}}</a></h5>
                        <input type="hidden" class="map-title-name" value={{this.title}} />
                        <p class="card-text">Formato: {{this.format}}</p>
                        <p class="card-text">Capacidade: {{this.capacity}} pessoas</p>

                        <p class="card-text"><small class="text-muted">{{this.address}}</small></p>
                    </div>
                </div>
            </div>
        </div>
        <div>
            <input type="hidden" class="mark-long" value="{{this.location.coordinates.[0]}}" />
            <input type="hidden" class="mark-lat" value="{{this.location.coordinates.[1]}}" />
        </div>
        {{/each}}
    </div>
    <div class="unless" style="max-width: 540px;">
        {{#unless spots }}
        <div class="o-bg-grey-5 py-5">
            <div class="ml-5 o-center justify-content-center text-center">
                <img src="/images/img-semresultado.png">
                <h2>Ops!</h2>
                <h4>O espaço com as características que você está buscando não foi encontrado. Teremos mais espaços em
                    breve.</h4>
            </div>
        </div>
        {{/unless}}
        <div class="card mb-3" id="search-index" style="max-width: 540px;">
            <a href="/" class="btn btn-primary justify-content-center">Voltar para a Busca</a>
        </div>

    </div>
</div>
<div id="map"></div>
<script src="https://maps.googleapis.com/maps/api/js?key={{googlekey}}"></script>